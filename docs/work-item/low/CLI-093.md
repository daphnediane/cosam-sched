# CLI-093: cosam-modify set command

## Summary

Implement the `set` subcommand to update a named field on one or more entities.

## Status

Open

## Priority

Low

## Blocked By

- CLI-092: cosam-modify list and get commands

## Description

`set` updates a single field on the selected entities, routing the change through
`EditContext::update_field_cmd` so the edit is tracked in the undo stack and mirrors to the
CRDT document.

```text
cosam-modify --file <path> [--select <type> [<query>]] set <field> <value>
```

### Field name resolution

`update_field_cmd` requires `field: &'static str`. Resolution: `E::field_set().get_by_name(name)`
→ `resolved.name()` (which is `&'static str`).

### Value coercion

Always pass `FieldValue::Single(FieldValueItem::String(raw.to_owned()))`. The field's write
callback handles cross-type coercion (String→Integer, String→Boolean, String→DateTime, etc.)
via the `IntoFieldValue` system.

### Multi-entity set

When the query matches multiple entities, wrap all `UpdateField` commands in a `BatchEdit`
so the entire operation is a single undo step.

## Acceptance Criteria

- [ ] `set name "New Name"` on a single entity updates the field and saves correctly
- [ ] Setting an unknown field name returns a clear error
- [ ] Multi-entity set (wildcard query) wraps in BatchEdit
- [ ] `cargo test` passes
