# CLI-098: cosam-modify help text, exit codes, integration tests, and polish

## Summary

Add `--help` output, proper exit codes, integration tests for all commands, and close out
CLI-031 and CLI-090–098.

## Status

Completed

## Priority

Low

## Blocked By

- CLI-097: cosam-modify undo, redo, and show-history commands

## Description

Final polish pass for the `cosam-modify` implementation.

### Help text

`--help` / `-h` prints usage to stdout and exits `0`. Each subcommand and option is
described with examples.

### Exit codes

- `0` — success
- `1` — user error (bad args, entity not found, validation failure)
- `2` — I/O or file format error

### Integration tests (`apps/cosam-modify/tests/`)

- Create→list round-trip for each entity type
- Set→get field value round-trip
- Delete and verify gone from list
- Add-edge / remove-edge
- Undo/redo within an invocation
- xlsx→binary conversion (load xlsx, save binary, reload binary, verify data)

### Cleanup

- `cargo clippy -- -D warnings` clean
- `cargo fmt`
- Mark CLI-031, CLI-090–CLI-098 as Completed; run `scripts/work-item-update.pl`

## Acceptance Criteria

- [x] `--help` prints usage without error
- [x] All integration tests pass
- [x] `cargo clippy` clean
- [x] All work items CLI-031 and CLI-090–098 marked Completed
